<%= form_for(@mash_up) do |f| %>
  <% if @mash_up.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mash_up.errors.count, "error") %> prohibited this mash_up from being saved:</h2>

      <ul>
      <% @mash_up.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
